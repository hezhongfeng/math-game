# 数感闯关设计规范

> 软件全称：数感闯关儿童数学启蒙训练软件｜产品简称：数感闯关｜软件版本：V1.0

数感闯关面向学前大班及小学低年级儿童，采用移动优先、儿童友好、简约科技风。界面设计必须服务于作答、反馈和数量理解，不为装饰增加复杂度。

## 核心原则

1. **功能优先**：颜色、动画、阴影和形状必须有明确用途。
2. **视觉克制**：蓝绿冷色为主，黄色用于奖励，红色仅用于错误。
3. **任务聚焦**：作答页优先保证题目、键盘和反馈清晰。
4. **移动优先**：默认适配手机竖屏，再增强平板和桌面布局。
5. **儿童友好**：文字短、数字大、触摸目标宽容、错误反馈不带惩罚感。
6. **可降级**：音频、振动、PWA 或 Three.js 高级交互不可用时，不阻塞核心训练。

数字探索使用 3D 球阵是明确的数位教学工具，不属于装饰性 3D 效果。

## 设计令牌

主要令牌位于 `src/assets/styles/variables.css`，Tailwind 映射位于 `tailwind.config.js`。

核心颜色：

| 语义 | CSS 变量 | 当前值 |
|------|----------|--------|
| 主操作 | `--brand-primary` | `#4A90E2` |
| 成功 | `--brand-success` | `#00D084` |
| 奖励 | `--brand-reward` | `#FFC700` |
| 警告 | `--brand-warning` | `#FF9F43` |
| 错误 | `--brand-alert` | `#FF6B6B` |
| 点缀 | `--brand-accent` | `#A29BFE` |

圆角、阴影、字体和动效时长也统一由 CSS 变量控制。新增样式优先复用现有变量，不复制硬编码主题值。

项目同时使用：

- Tailwind 工具类处理通用布局和少量响应式样式。
- Vue scoped CSS 处理复杂组件布局和状态。
- `style.css` 处理全局基础样式。
- `assets/styles/animations.css` 处理可复用动画。

不要假设所有样式都必须写成 Tailwind。

## 移动端规范

视口配置：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```

不得通过 viewport 或 JavaScript 禁止双指缩放。`touch-action: manipulation` 只用于减少操作延迟，不替代浏览器缩放能力。

触摸要求：

- 所有交互元素至少 `44×44px`。
- 数字键盘目标尺寸至少 `64×64px`。
- 主要操作按钮至少 48px 高。
- 使用 `touch-action: manipulation`。
- 使用 `-webkit-tap-highlight-color: transparent`。

安全区域：

```css
padding-top: max(10px, env(safe-area-inset-top));
padding-bottom: max(24px, env(safe-area-inset-bottom));
```

页面允许正常纵向滚动，禁止横向溢出。不要通过全局锁滚动解决局部布局问题。

## 响应式布局

实际关键断点：

| 条件 | 用途 |
|------|------|
| `< 360px` | 极窄手机压缩字体和间距 |
| `≤ 420px` | 常规手机优化 |
| `< 960px` | 单列游戏布局，键盘位于状态区之前 |
| `≥ 960px` | 游戏页题目/控制双列布局 |
| Tailwind `md` / `lg` | 其他页面的渐进增强 |

不要把 `md:` 视为全项目唯一断点来源；当前核心页面大量使用 scoped media query。

## 游戏页

薄弱题直接混入正常闯关，与普通题使用相同视觉和交互，不增加独立训练入口、“薄弱题”标签或装饰状态。

### 题目卡

- 卡片在正常换题时保持稳定。
- 只对算式内容做约 150ms 的透明度、小位移过渡。
- 计数器和小球开关不随算式滑出。
- 算式舞台固定高度，不能导致数字键盘上下跳动。

### 小球辅助

- 默认关闭，开关只在当前题目可表示时出现。
- 加法、减法、缺项题使用不同状态，但不显示长策略文案。
- 每行 10 个位置，最多 30 个。
- 开关时平滑展开/收起题目卡高度。

### 反馈

- 正确：轻量成功反馈，延时后自动进入下一题。
- 错误：全屏反馈层，显示正确答案，用户点击后继续。
- 连对：仅在第 3 次和之后每 5 次里程碑显示短提示。
- 结算：先展示结果和榜单，需要时再进入错题复盘。

## 动画

只动画 `transform` 和 `opacity`；确需平滑布局变化时，可对受控的 `max-height`、margin、padding 做短过渡。

| 场景 | 建议 |
|------|------|
| 按钮按压 | 轻微缩放，约 150ms |
| 算式换题 | 交叉淡入淡出 + 8-10px 位移，约 150ms |
| 小球开关 | 高度 + 透明度，约 250ms |
| 页面/卡片入场 | 只在首次进入时使用 |
| 错误反馈 | 温和摇动，避免高频重复 |

全局 `prefers-reduced-motion: reduce` 会把动画和过渡压缩到近乎即时。新增动画必须兼容该规则。

## 无障碍

- 优先使用语义化按钮、标题和 section。
- 图标按钮必须提供 `aria-label`。
- 开关使用 `aria-pressed` 或对应语义。
- 不依赖颜色作为唯一状态信号。
- 数字使用稳定的 tabular figures。
- 焦点态必须可见。
- 不要禁用键盘交互来换取触摸体验。
- 页面提供跳到主内容的链接，标题保持合理层级。
- 模态界面使用 `role="dialog"`、可读标题和焦点约束；打开时隔离背景，关闭后恢复原焦点。
- Toast、加载和练习反馈等异步状态通过合适的 live region 对辅助技术播报。
- Three.js 等视觉教学内容必须提供等价的文字描述。

## 音频与触觉

- 音效和振动默认开启，目前没有设置开关。
- 音频失败时静默降级，不能阻止提交或导航。
- Vibration API 不可用时静默忽略。
- 每轮只播放一条结算主反馈语音。
- 真机 iOS Safari 的自动播放限制必须通过用户交互场景验证。

## 数字探索

- 球阵用于表达个位、十位和百位结构。
- 移动端自动旋转应缓慢，不能干扰页面滚动。
- 桌面端可旋转和缩放，但不应遮挡输入和模式切换。
- 数量、拆解文字和球阵必须表达同一个值。

## 验证清单

UI 改动至少验证：

- 320px、390px 和桌面宽度无横向溢出。
- 数字键盘和主要按钮触摸面积足够。
- 刘海屏安全区域正常。
- 换题、小球辅助和反馈不会引发布局跳动。
- `prefers-reduced-motion` 下核心操作仍清楚。
- 正确、错误、锁定和禁用状态不只依赖颜色。
- iOS Safari / Android Chrome 真机关键流程可用。

构建体积和浏览器兼容性以当前构建结果及真机测试为准，不在文档中长期维护未经持续验证的固定分数或版本承诺。
